[pytest]
testpaths = tests
markers =
    smoke: core paths that must work before anything else is worth running
    negative: expected failures the application must handle gracefully
    a11y: WCAG checks via axe-core
addopts = -v --strict-markers
